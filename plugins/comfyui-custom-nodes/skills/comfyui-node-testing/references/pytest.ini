[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short

markers =
    unit: Unit tests (fast, no server required)
    integration: Integration tests (requires ComfyUI server on port 8188)
